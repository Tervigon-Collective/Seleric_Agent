# 10 — Completion Gate

Completion ≠ all agents finished.

Requires: objectives satisfied, blocking subquestions terminal, required claims validated, conflicts resolved, Skeptic gates passed, synthetic policy satisfied.

`CompletionDecision` records coverage, blockers, claim buckets, and reasons.
