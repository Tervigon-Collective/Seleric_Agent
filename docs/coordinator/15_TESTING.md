# 15 — Testing

```bash
uv run pytest tests/coordinator -q
uv run pytest -q
```

Coverage includes lookup minimum swarm, executive health decomposition, CAC recursive refine, ruled-out branches, Skeptic follow-ups, dedup, leadership loop, PASS/REVISE/REJECT, causal-graph remediation, synthetic taint, budget partial, completion gate, swarm_v2 CAC integration, LangGraph DECIDE→EXECUTE wave events, health+forecast+action intent alignment, and API scenario matrix (`full_*` intent forcing, terminal status).
