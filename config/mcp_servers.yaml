servers:
  # Live Seleric catalogue + metrics_query. Sprint 5: MCPGateway no longer
  # pins a per-domain module from agent_registry.yaml (deleted); the V3
  # agent calls unscoped unless the tool args pass module explicitly.
  seleric:
    capability_prefix: seleric
    transport: streamable_http
    url_env: SELERIC_MCP_URL
    auth_token_env: SELERIC_MCP_TOKEN
