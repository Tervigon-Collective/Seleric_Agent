servers:
  # Live Seleric catalogue + metrics_query. Domain scope is the `module`
  # argument MCPGateway pins from config/agent_registry.yaml.
  seleric:
    capability_prefix: seleric
    transport: streamable_http
    url_env: SELERIC_MCP_URL
    auth_token_env: SELERIC_MCP_TOKEN
